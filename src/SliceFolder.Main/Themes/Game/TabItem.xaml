<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:component="clr-namespace:SliceFolder.Main.Game.Components">

    <Style TargetType="{x:Type component:TabItem}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type component:TabItem}">
                    <Grid
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border
                            x:Name="bdr"
                            Height="{Binding ElementName=PART_Content, Path=ActualHeight}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            BorderBrush="#428cf7"
                            BorderThickness="1.5"
                            Visibility="Hidden" />
                        <ContentPresenter
                            x:Name="PART_Content"
                            Grid.Column="1"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bdr" Property="Visibility" Value="Visible" />
                            <Setter Property="Background" Value="#212834" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>